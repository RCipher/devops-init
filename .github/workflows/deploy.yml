echo "🔹 Подключились к серверу!"

# Создаем директорию, если не существует
mkdir -p ~/devops_init/docker-projects
cd ~/devops_init/docker-projects

# Проверяем, является ли текущая директория git-репозиторием
if [ ! -d .git ]; then
  echo "🔹 .git не найден, клонируем репозиторий..."
  git clone https://github.com/RCipher/devops-init.git .
else
  echo "🔹 Репозиторий уже инициализирован."
fi

# Проверяем, нет ли незакоммиченных изменений
if [ -n "$(git status --porcelain)" ]; then
  echo "⚠️ Найдены незакоммиченные изменения. Сохраняем их в stash."
  git stash
fi

echo "🔹 Тянем свежий код из GitHub..."
git pull origin main --ff-only

# Проверяем наличие ошибок после pull
if [ $? -ne 0 ]; then
  echo "❌ Ошибка при обновлении репозитория!"
  exit 1
fi

echo "✅ Репозиторий успешно обновлен!"
