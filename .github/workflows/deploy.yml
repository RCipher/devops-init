echo "🔹 Подключились к серверу!"
mkdir -p ~/devops_init/docker-projects
cd ~/devops_init/docker-projects

# Проверяем, есть ли .git, если нет — клонируем репозиторий
if [ ! -d .git ]; then
  git clone https://github.com/RCipher/devops-init.git .
fi

echo "🔹 Тянем свежий код из GitHub..."
git pull origin main
